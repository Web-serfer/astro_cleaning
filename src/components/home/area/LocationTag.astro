---
interface Props {
    name: string;
}

const { name } = Astro.props;
---

<span class="inline-block px-3 py-1 text-xs font-medium uppercase tracking-wider text-gray-600 border border-dashed border-gray-400 rounded-full whitespace-nowrap">
  {name}
</span>
